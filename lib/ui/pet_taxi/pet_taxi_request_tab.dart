import 'package:flutter/material.dart';

class PetTaxiRequestTab extends StatelessWidget {
  const PetTaxiRequestTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pet Taxi Request'),
    );
  }
}