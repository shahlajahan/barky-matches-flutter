import 'package:flutter/material.dart';

class DriverLayer extends StatelessWidget {
  const DriverLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
