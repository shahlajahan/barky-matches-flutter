# Task 0002: lib/dog_card.dart

## Target file

lib/dog_card.dart

## Findings

- `475:27` **warning** Probable user-visible hardcoded string in Text(data: ...). ('Boost plans are temporarily unavailable.') `[flutter.localization.hardcoded-ui-string]`

## Suggested objective

Only make the changes described below. Do not alter unrelated logic, tests, or formatting.
- Move this text into the project's localization resources instead of hardcoding it.
