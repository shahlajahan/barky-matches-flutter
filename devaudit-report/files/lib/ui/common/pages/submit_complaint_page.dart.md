# lib/ui/common/pages/submit_complaint_page.dart

11 finding(s).

- `50:29` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Submit Complaint") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `52:13` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Are you sure you want to submit this complaint?") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `56:33` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Cancel") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `60:33` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Submit") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `90:38` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Complaint submitted successfully") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `122:53` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Unexpected error") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `144:27` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Submit Complaint") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `160:30` **warning** Probable user-visible hardcoded string in InputDecoration(labelText: ...). ("Category") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `181:30` **warning** Probable user-visible hardcoded string in InputDecoration(labelText: ...). ("Title") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `202:30` **warning** Probable user-visible hardcoded string in InputDecoration(labelText: ...). ("Description") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
- `240:27` **warning** Probable user-visible hardcoded string in Text(data: ...). ("Submit Complaint") `[flutter.localization.hardcoded-ui-string]`
  - Suggestion: Move this text into the project's localization resources instead of hardcoding it.
