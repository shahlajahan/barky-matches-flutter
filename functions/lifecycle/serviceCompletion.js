async function markServiceCompleted({
    booking,
}) {

    // save completedAt

    // update booking status

    // trigger settlement evaluation

}

module.exports = {
    markServiceCompleted,
};