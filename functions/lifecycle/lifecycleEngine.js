const {
    markServiceCompleted,
} = require("./serviceCompletion");

const {
    updateSettlementEligibility,
} = require("./settlementUpdater");

module.exports = {
    markServiceCompleted,
    updateSettlementEligibility,
};