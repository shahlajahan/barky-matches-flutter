const {
    isEligibleForSettlement,
} = require("../settlement");

async function updateSettlementEligibility(
    booking
) {

    // evaluate settlement

}

module.exports = {
    updateSettlementEligibility,
};